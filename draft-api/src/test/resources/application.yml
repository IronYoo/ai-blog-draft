spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  cloud:
    aws:
      credentials:
        access-key: test
        secret-key: test
      s3:
        region: test
        bucket: test
      endpoint: http://127.0.0.1:4566

image:
  url: https://test-image.mochayoo.site